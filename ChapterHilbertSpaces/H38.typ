#import "../template.typ": *

= 
Let $cal(A)$ be a positive semidefinite operator on $cal(H)$, i.e. $cal(A)$ is symmetric and $innerp(u, cal(A)u) >= 0$ for all $u in D_cal(A)$. Show that the eigenvalues of $cal(A)$ all are $>= 0$

#solution[
  
]